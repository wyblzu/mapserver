spring:
  profiles:
    active: dev
  devtools:
    restart:
      enabled: true
  datasource:
    url: jdbc:postgresql://localhost:5432/opengis
    username: wyblzu
    password: 119889

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8000/eureka

logging:
  path: ./logs/spatial-query
  level:
    com.wyb.spatialquery: debug
    org.springframework.web: debug

swagger2:
  enabled: true