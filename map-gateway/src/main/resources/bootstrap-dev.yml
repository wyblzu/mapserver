spring:
  profiles:
    active: dev
  devtools:
    restart:
      enabled: true

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8000/eureka


logging:
  path: ./logs/map-gateway
  level:
    com.wyb.gateway: warn
    org.springframework.web: warn